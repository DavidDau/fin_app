{$FINAPP_DOMAIN} {
    encode zstd gzip

    # Keep the API and browser application on the same HTTPS origin.
    handle /health {
        reverse_proxy api:8000
    }

    handle /api/* {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
